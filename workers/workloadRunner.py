from classes import *

workloadHandler = WorkloadManager('workloads/')

workloadHandler.runWorkloads(
                            state = 'error'
                            )

#workloadHandler.findWorkload(1701210615.561919)
